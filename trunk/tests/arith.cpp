#include <iostream>
using namespace std;

int main() {
  int a = 37;
  a = ( (a * (a * a)) / ( ( (40 - ( (a && 9) || 127 ) ) * (-5) ) + ( (-a) * (!5) ) ) );
  a = ( a + (- ( a < 0 ) ) );
  a = ( a + ( a > 500 ) );
  a = ( a == ( -260) );
  cout<< a << endl;
}
