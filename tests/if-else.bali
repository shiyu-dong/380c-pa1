int method1(int a)
{
    int b;
    if( (a>0) )
      if ( ( (a = 2) ) ) { // 2 goes here
        b = 1;
        b = (b*2);
      }
      else  // 5 goes here
        return a;
    else {
      if ( ( a = (-2) ) ) { // -2 goes here
        b = (-1);
        b = ( (-b) * (-2));
      }
      else {
        if ( (a = (-1)) ) //-1 goes here
          b = (-1);
        else // -10 goes here
          return a;
      }
    }
    return b;

}

int main()
{
  //return(method1(2));
  //return(method1(5));
  //return(method1((-2)));
  //return(method1((-1)));
  return(method1((-10)));
}
